{
  "DATA": {
    "COMMAND": [
      {
        "@_Name": "Suppress",
        "@_Value": "(doy == 358 || doy == 357 || doy == 4 || doy == 5)",
        "@_Comment": "Suppress sequential readings on eve and day of Nativity and Theophany"
      },
      {
        "@_Name": "Suppress",
        "@_Value": "(doy == 355 || (doy == 356 && dow != 1) || (doy == 357 && (dow >= 3 || dow == 0)) || (doy == 358 && (dow >= 4 || dow == 0)) || (doy == 359 && (dow >= 5 || dow == 0)) || (doy == 360 && (dow == 6 || dow == 0)) || (doy == 361 &&  dow == 0)) && (ndayF >= -125 && ndayF <= -105)",
        "@_Comment": "This suppresses the readings that occur over Christmas in the case that there are 3 repeats and it is the 31st week of effective readings.",
        "@_Cmd": "GS == 1"
      },
      {
        "@_Name": "Suppress",
        "@_Value": "(doy == 362 || (doy == 363 && dow != 1) || (doy == 364 && (dow >= 3 || dow == 0)) || (doy == 361 && dow != 0) || (doy == 360 && (dow < 6 && dow != 0)) || (doy == 359 && (dow < 5 && dow != 0)) || (doy == 358 && (dow < 4 && dow != 0)) || (doy == 357 && (dow < 3 && dow != 0)) || (doy == 356 && (dow == 1))) && (ndayF >= -118 && ndayF <= -81)",
        "@_Comment": "This suppresses the readings that occur over Christmas in the case that there are 2 repeats and it is the 32nd week of effective readings.",
        "@_Cmd": "GS == 1"
      },
      {
        "@_Name": "Suppress",
        "@_Value": "((doy == 0 && (dow >= 4 || dow == 0)) || (doy == 1 && (dow >= 5 || dow == 0)) || (doy == 2 && (dow == 6 || dow == 0)) || (doy == 3 &&  dow == 0)) &&  (nday >= -118 && nday <= -81)",
        "@_Comment": "This suppresses the readings that occur over Christmas in the case that there are at least 2 repeats and it is the 32nd week of effective readings and it is January.",
        "@_Cmd": "GS == 1"
      },
      {
        "@_Name": "Suppress",
        "@_Value": "(doy == 4 || (doy == 5 && dow != 1) || (doy == 6 && (dow >= 3 || dow == 0)) || (doy == 7 && (dow >= 4 || dow == 0)) || (doy == 8 && (dow >= 5 || dow == 0)) || (doy == 9 && (dow == 6 || dow == 0)) || (doy == 10 &&  dow == 0)|| (doy == 3 && dow != 0) || (doy == 2 && (dow < 6 && dow != 0)) || (doy == 1 && (dow < 5 && dow != 0)) || (doy == 0 && (dow < 4 && dow != 0))) && (nday <= -78 && nday >= -111)",
        "@_Comment": "This suppresses the readings for the case that there is only 1 repeat and it is the 33rd week of effective readings",
        "@_Cmd": "GS == 1"
      },
      {
        "@_Name": "Suppress",
        "@_Value": "((doy == 364 && (dow < 3 && dow != 0)) || (doy == 363 && (dow == 1))) && (ndayF <= -78 && ndayF >= -111)",
        "@_Comment": "This suppresses the readings for the case that there is at least 1 repeat and it is the 33rd week of effective readings and it is still the old year.",
        "@_Cmd": "GS == 1"
      },
      {
        "@_Name": "AcceptTransfers",
        "@_Value": "dow != 0 && dRank < 5 && !(nday < 0 && nday > -63)"
      },
      {
        "@_Name": "TransferRulesB",
        "@_Value": "dow != 0 && dow != 1 && dRank >= 5 && !(nday < 0 && nday > -63)"
      },
      {
        "@_Name": "TransferRulesF",
        "@_Value": "dow == 1 && dRank >= 5 && !(nday < 0 && nday > -63)"
      }
    ]
  }
}